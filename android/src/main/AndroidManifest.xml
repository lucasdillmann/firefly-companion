<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Main">
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".biometric.BiometricUnlockActivity"/>
        <activity android:name=".home.HomeActivity"/>
        <activity android:name=".onboarding.OnboardingServerFormActivity"/>
        <activity android:name=".onboarding.OnboardingStartActivity"/>
        <activity android:name=".onboarding.OnboardingPreferencesFormActivity"/>
        <activity android:name=".preferences.PreferencesActivity"/>
    </application>
</manifest>
